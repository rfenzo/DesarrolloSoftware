<h2>Mis Donaciones</h2>
<hr>
<% if @donations.any? %>
    <h4 class="text-right">
      Total:
      <span class="font-weight-bold">$ <%=@total_amount%></span>
    </h4>

  <div class="list-group-item border bg-dark text-light font-weight-bold">
    <div class="row">
      <div class="col-2 m-auto text-center">
        <%= t(:date)%>
      </div>
      <div class="col-8 m-auto text-center">
        <%= t(:one, scope: %i[project name]) %>
      </div>
      <div class="col m-auto text-center">
        <%= t(:amount) %>
      </div>
    </div>
  </div>
  <% @donations.each do |donation| %>
    <div class="list-group-item border">
      <div class="row">
        <div class="col-2 m-auto text-center">
          <%= donation.created_at.day %>-<%= donation.created_at.month %>-<%= donation.created_at.year %>
        </div>
        <div class="col-8 m-auto text-center">
          <%= link_to donation.project.name, project_path(donation.project) %>
        </div>
        <div class="col m-auto text-center">
          $ <%= donation.amount %>
        </div>
      </div>
    </div>
  <% end %>
<% else %>
<div class="alert alert-info">
  <h5 class="alert-heading">
    Aún haz realizado donaciones
  </h5>
  <hr>
  <div class="">
    Una vez que realices donaciones, podrás verlos aquí y comenzarás a ganar reconocimiento
  </div>
</div>
<% end %>
